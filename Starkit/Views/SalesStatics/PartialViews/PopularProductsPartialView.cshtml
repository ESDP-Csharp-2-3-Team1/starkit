@model System.Collections.ArrayList

@if (Model.Count != 0)
{
    @foreach (var popularProduct in Model)
    {
        <tr>
            @foreach (var p in popularProduct.GetType().GetProperties())
            {
                <td>@p.GetValue(popularProduct, null)</td>
            }
        </tr>   
    }
}